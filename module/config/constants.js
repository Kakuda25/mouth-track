/**
 * アプリケーション定数定義
 */

// MediaPipe FaceMeshの口ランドマークインデックス（8点：外側のみ）
export const MOUTH_LANDMARKS = {
    leftEnd: 61,        // 左端
    rightEnd: 291,      // 右端
    topOuter: 13,       // 上唇中央（外側）
    bottomOuter: 14,    // 下唇中央（外側）
    topLeft: 37,        // 上唇外側左
    topRight: 267,      // 上唇外側右
    bottomLeft: 84,     // 下唇外側左
    bottomRight: 314    // 下唇外側右
};

// MediaPipe FaceMeshの口周辺の全ランドマークインデックス
// refineLandmarks: true の場合に利用可能な詳細なランドマーク
// 重複を削除し、一意のインデックスのみを保持
export const MOUTH_ALL_LANDMARKS = [
    // 上唇（外側）
    12, 13, 14, 15, 16, 17, 18,
    // 上唇（内側）
    78, 79, 80, 81, 82,
    // 下唇（内側）
    308, 309, 310, 311, 312,
    // 口角
    61, 291,
    // その他の口周辺の点（重複を削除）
    0, 11, 200, 269, 270, 271, 272, 273, 274, 275, 276, 277, 278, 279, 280, 281, 282, 283, 284, 285, 286, 287, 288, 289, 290, 292, 293, 294, 295, 296, 297, 298, 299, 300, 301, 302, 303, 304, 305, 306, 307, 313, 314, 315, 316, 317, 318, 319, 320, 321, 322, 323, 324, 325, 326, 327, 328, 329, 330, 331, 332, 333, 334, 335, 336, 337, 338, 339, 340, 341, 342, 343, 344, 345, 346, 347, 348, 349, 350, 351, 352, 353, 354, 355, 356, 357, 358, 359, 360, 361, 362, 363, 364, 365, 366, 367, 368, 369, 370, 371, 372, 373, 374, 375, 376, 377, 378, 379, 380, 381, 382, 383, 384, 385, 386, 387, 388, 389, 390, 391, 392, 393, 394, 395, 396, 397, 398, 399, 400, 401, 402, 403, 404, 405, 406, 407, 408, 409, 410, 411, 412, 413, 414, 415, 416, 417, 418, 419, 420, 421, 422, 423, 424, 425, 426, 427, 428, 429, 430, 431, 432, 433, 434, 435, 436, 437, 438, 439, 440, 441, 442, 443, 444, 445, 446, 447, 448, 449, 450, 451, 452, 453, 454, 455, 456, 457, 458, 459, 460, 461, 462, 463, 464, 465, 466, 467
].filter((value, index, self) => self.indexOf(value) === index); // 重複を削除

// より正確な口周辺のランドマーク（MediaPipe FaceMeshの標準的な口の輪郭）
// 16点版（既存、後方互換性のため残す）
export const MOUTH_CONTOUR_INDICES = [
    // 上唇外側
    12, 13, 14, 15, 16, 17, 18,
    // 上唇内側
    78, 79, 80, 81, 82,
    // 下唇内側
    308, 309, 310, 311, 312,
    // 口角
    61, 291
];

// 拡張された口周辺のランドマーク（34点版）
// 母音判定精度向上のため、口角周辺、頬、顎の点を追加
// 下唇外側のランドマーク（84, 314）も追加して正確な口の輪郭を描画
// 注意: 変数名は後方互換性のため`MOUTH_CONTOUR_INDICES_32`のままですが、実際には34点です
export const MOUTH_CONTOUR_INDICES_32 = [
    // === 既存の16点 ===
    // 上唇外側（7点）
    12, 13, 14, 15, 16, 17, 18,
    // 上唇内側（5点）
    78, 79, 80, 81, 82,
    // 下唇内側（5点）
    308, 309, 310, 311, 312,
    // 口角（2点）
    61, 291,
    
    // === 下唇外側（2点） - 口の輪郭描画に重要 ===
    84,              // 下唇外側左
    314,             // 下唇外側右
    
    // === 追加の16点 ===
    // 口角周辺の追加点（6点）- 「い」「え」の判別に重要
    39, 40, 41,      // 左口角周辺
    269, 270, 271,   // 右口角周辺
    
    // 頬の動き（4点）- 「い」の発音時に重要
    116, 117,        // 左頬
    345, 346,        // 右頬
    
    // 顎の動き（4点）- 「あ」の発音時に重要
    175, 176,        // 顎中央
    172, 397,        // 顎左右
    
    // 口周辺の形状変化を捉える点（2点）
    2,               // 鼻下
    200              // 口周辺上部
];

// MediaPipe FaceMesh設定
export const FACE_MESH_CONFIG = {
    maxNumFaces: 1,
    refineLandmarks: true,
    minDetectionConfidence: 0.3,  // 顔検出の感度を上げる（0.5 → 0.3）
    minTrackingConfidence: 0.3    // トラッキングの感度を上げる（0.5 → 0.3）
};

// カメラ設定
export const CAMERA_CONFIG = {
    width: 1280,
    height: 720,
    facingMode: 'user'
};

// デフォルト設定値
export const DEFAULT_SETTINGS = {
    smoothingFactor: 0.5,
    showLandmarks: true,
    showCoordinates: false,
    mirrorMode: true  // 常にミラーモードを有効化
};

// FPS計算用
export const FPS_INTERVAL = 1000; // 1秒

// データエクスポート設定
export const EXPORT_CONFIG = {
    csvDelimiter: ',',
    dateFormat: 'YYYY-MM-DD_HH-mm-ss'
};

